#include <stdio.h>
#include "calculator.h"

int main() {
    printf("%d\n", add(10, 20));
    printf("%d\n", sub(10, 20));
    printf("%d\n", multi(10, 20));
    printf("%d\n", printAdd(10, 20));
    
    return 0;
}